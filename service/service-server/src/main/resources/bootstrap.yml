spring:
  cloud:
    config:
      uri: http://localhost:8082/
      profile: default
      name: server
      label: eureka

---
# dev环境配置
spring:
  profiles: dev
  cloud:
    config:
      uri: http://localhost:8082/
      profile: dev
      name: server
      label: eureka