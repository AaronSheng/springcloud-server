spring:
  cloud:
    config:
      uri: http://localhost:8082/
      profile: default
      name: server
      label: master

---
# dev环境配置
spring:
  profiles: dev
  cloud:
    config:
      uri: http://localhost:8082/
      profile: dev
      name: server
      label: master