spring:
  cloud:
    config:
      uri: http://localhost:8082/
      profile: default
      name: client
      label: eureka

---
# dev环境配置
spring:
  profiles: dev
  cloud:
    config:
      uri: http://localhost:8082/
      profile: dev
      name: client
      label: eureka